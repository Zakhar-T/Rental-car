export const selectCarDetails = (state) => state.carDetails.car;
export const selectCarDetailsIsLoading = (state) => state.carDetails.isLoading;
export const selectCarDetailsError = (state) => state.carDetails.error;
